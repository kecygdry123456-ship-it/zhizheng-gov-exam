# 知政公考可用 MVP 最终验收报告

> 验收日期：2026-07-11  
> 结论：通过，可作为本地可用首版继续试用和迭代

## 1. 最终检查结果

| 检查项 | 结果 |
| --- | --- |
| ESLint | 通过 |
| Prisma Schema 校验 | 通过 |
| TypeScript 检查 | 通过 |
| Next.js 生产构建 | 通过 |
| 接口集成测试 | 29 项通过（含第三方模型与全局模型配置回归） |
| 浏览器流程测试 | 16 项通过（含图片题渲染、跳题和移动端流程） |
| 测试总数 | 45 项通过，0 失败 |
| PostgreSQL 服务 | 正常 |
| 服务状态接口 | 正常 |
| 独立空数据库迁移演练 | 通过 |

完整验证命令：

```bash
npm test
```

## 2. 已验收的学员流程

- 在登录页切换到注册模式，使用姓名或昵称、邮箱、密码、确认密码和可选目标考试创建账号。
- 注册邮箱会去除首尾空白并统一转为小写；注册成功后自动创建会话并进入学习首页。
- 公开注册无论是否携带角色字段都只创建 `STUDENT` 学员账号，响应不暴露密码哈希。
- 重复邮箱、弱密码、超过 bcrypt 72 字节限制的密码和两次密码不一致均返回明确的输入错误。
- 使用演示账号登录。
- 刷新页面后恢复登录状态。
- 从 PostgreSQL 读取已发布题目。
- 从 12224 道可用题目中按条件随机组题，并显示地区、年份和试卷来源。
- 资料分析共 1035 道、207 份完整材料，每份材料严格关联 5 道题且组题时不可拆分。
- 每道题显示 1～10 分难度系数；专项训练支持选择板块、题量和难度范围。
- 模拟考试只提供国考型（130 题/120 分钟：政治20、常识15、言语30、数量10、判断35、资料20）和省考型（90 题/90 分钟：政治10、常识5、言语15、数量15、判断25、资料20），题量、时长、板块配额和顺序均固定。
- 专项配置持久化到账号并提供推荐难度；模拟考试记忆最近选择的固定卷型。
- 模拟考试逐题展示、提供答题导航、实时保存答案并支持刷新恢复。
- 专项练习按题目保存本轮作答状态，返回已答题时恢复答案与解析并禁止重复提交、重复计分。
- 资料分析在综合组题中不再强制插入卷首；正式模拟卷按板块顺序排列，资料题保持完整五题材料组。
- 政治板块从核心理论题型白名单抽取，常识板块使用互斥的非政治题型白名单；正式卷同时过滤占位内容并对整卷题干去重。
- 提交答案并由服务端判定。
- 提交前不返回答案和解析。
- 保存作答记录。
- 收藏和取消收藏题目。
- 错误作答进入错题列表。
- 查看真实累计答题、正确率、模块能力和七天趋势。
- 进入模拟考试、选择答案、统一交卷并查看成绩。
- 退出登录并清除会话。
- 在 390×844 移动端视口完成核心导航和练习。
- 学员界面不显示题库管理入口。
- 填写目标、时间、阶段和重点模块后生成个性化七天计划。
- 可选使用用户本次输入的 API Key 生成模型增强计划，Key 不持久化。

## 3. 已验收的管理员流程

- 管理员登录后显示题库管理入口。
- 读取完整管理题库。
- 按题干、题型或分类搜索。
- 通过页面表单新增题目。
- 编辑题干、选项、答案、解析、分类、难度和状态。
- 发布题目后同步到学员题库。
- 停用题目后从学员题库移除。
- 学员无法调用管理员写入接口。
- 不完整题目数据会被明确拒绝。

## 4. 数据与接口验收

- 登录、分页、答题和题目表单均具备输入校验。
- 题目列表支持分页和分类、题型、难度筛选。
- 公开题目按外部编号去重，来源、地区、年份和试卷信息可追踪。
- 缺少公共材料或依赖图片的题目在导入阶段被过滤。
- 模拟考试使用一次性服务端事务写入，避免部分答案写入。
- 收藏由用户和题目唯一约束保证一致性。
- 错题和统计始终限定当前登录用户。
- 统计日期边界明确使用 Asia/Shanghai。
- `GET /api/health` 可以检查服务和数据库状态。
- 测试数据会在测试结束后自动清理。

## 5. 前端工程与视觉验收

原先集中在 `exam-app.tsx` 的页面已拆分为：

- `app-shell.tsx`
- `login-view.tsx`
- `dashboard-view.tsx`
- `practice-view.tsx`
- `exam-view.tsx`
- `wrong-view.tsx`
- `statistics-view.tsx`
- `admin-view.tsx`
- `types.ts`
- `ui.tsx`

旧版练习组件和固定题目数组已删除。页面统计使用真实数据，管理端操作不再是本地假操作。

本轮进一步统一了整套前端视觉系统：

- 登录与注册在同一认证页面完成，字段具有显式标签、自动填充语义、密码显隐和可访问的错误提示。
- 桌面端采用统一品牌区、侧边导航、用户区和学习目标区域；移动端继续提供可展开导航。
- 学习首页使用层级清晰的欢迎区、统计卡和快捷入口，核心业务文案与导航标识保持兼容。
- 专项训练、模拟考试、智能规划、申论、错题、统计和题库管理复用统一的按钮、输入框、卡片、加载与空状态样式。
- 交互控件具备清晰的键盘焦点状态，移动端主要控件满足最小触控尺寸，并兼容减少动画偏好。
- 注册页在 390px 及更窄视口下可纵向滚动，不出现横向溢出或顶部表单截断。

## 6. 自动化覆盖

接口测试覆盖：

- 服务状态。
- 注册、登录和退出。
- 注册邮箱规范化、密码边界、重复账号、角色提升防护和敏感字段保护。
- 当前用户。
- 题目字段保护。
- 分页输入边界。
- 服务端判题。
- 模拟考试提交。
- 收藏和错题。
- 学习统计。
- 学员与管理员角色边界。
- 管理员新增、编辑、发布和停用题目。

浏览器测试覆盖：

- 学员注册、自动登录、退出后重新登录。
- 学员登录、练习、统计和退出。
- 登录/注册模式切换和窄屏注册表单无横向溢出。
- 管理员进入题库管理并通过表单新增题目。
- 移动端导航和横向布局。
- 学员模拟考试和成绩报告。
- 专项练习最后一题进入完成报告，不再回到第一题。
- 千题规模随机行测练习及五大模块覆盖。
- 单题难度标注、非简单平均的套题难度计算和难度范围筛选。
- 申论材料读取、在线作答和评分反馈。
- 七天智能学习计划生成与持久化。
- 完整题库不少于 1000 道，随机组题接口每次返回 50 道。
- 自定义数量关系专项训练和自定义 5 题、10 分钟模拟考试。
- 完整资料分析材料显示与五题题组约束。
- 未交卷模拟考试刷新后恢复题目、倒计时和已选答案。
- 个性化规划参数与 API Key、模型标识组合校验。

## 7. 可继续增强但不阻塞使用的事项

- 增加邮箱验证、修改密码、找回密码、登录限流和异常登录审计。
- 增加正式试卷编排与考试会话恢复。
- 增加错题掌握状态和间隔复习计划。
- 增加申论题库和智能批改。
- 增加生产部署、数据库自动备份和运行监控。
- 将正式环境的数据库变更全部纳入 Prisma Migration。

上述事项属于下一阶段产品增强，不影响当前本地 MVP 的完整使用。

## 9. 2026-07-15 学习计划程序验收

本轮将学习计划从纯自我确认升级为可执行、可恢复的程序验收闭环：

- 新计划使用 `schemaVersion=4`，每项任务的 `completionSpec` 在模型输出归一化后由服务端重新派生；客户端和模型不能自行降低验收标准。
- 专项、限时测评、正式模考和申论使用程序验收；知识学习、错题、复盘继续使用双重自我验收；休整任务不计入完成率。
- 专项、模考和申论提交时写入 `planContext`，最终训练报告或申论提交记录继承计划 ID 与任务键。
- 验收接口只接收证据 ID，题量、正确率、服务端墙钟用时、题目难度、板块、正式卷型、申论字数和分数均从数据库重新计算。
- 资料分析通过 `TrainingReport.attemptIds -> Attempt.questionId -> Question.materialId` 核算，完整作答同一材料的 5 道题才计为一组。
- 程序证据使用独立 claim 表永久占用，不能跨任务复用；程序验收打卡不可手动撤销。
- 规则哈希、任务标题、目标或完成标准变化后，旧程序打卡自动失效并要求新证据。
- `schemaVersion<=3` 的旧计划保持原有自我验收，不受升级影响。

本轮验证结果：

- ESLint、TypeScript、Prisma schema 与生产构建通过。
- 单元测试 `25/25` 通过。
- API 集成测试 `47/47` 通过。
- Playwright UI 测试 `27/27` 通过，覆盖 320px 与 390px。
- 联合 API/UI 回归 `74/74` 通过，总计 `99` 项自动化检查通过。
- 独立安全复审未发现未解决的 P0、P1 或 P2 问题。

云端发布与线上验收：

- 线上地址：`https://8.163.38.217`
- 应用镜像：`sha256:0fac30bb6637ba7a713adf9a671505070c1eb6e48b911b65e30193924ac35bc8`
- 发布前备份：`/opt/govexam/backups/pre-20260715-program-acceptance-r1-20260715-230043.dump`
- 备份大小：`10,683,189` 字节
- 迁移数量：`12`
- 发布前后核心数据计数一致：用户 `1`、题目 `20,683`、作答 `119`、训练报告 `4`、计划 `6`、打卡 `5`
- 线上临时学员完成了真实 5 题专项、训练报告生成和 `PROGRAM_VERIFIED` 验收；验收后临时用户及其关联数据已级联删除。
- 390px 线上页面无横向溢出，系统验收状态和证据摘要可见。

## 8. 部署演练结果

验收时创建了独立空数据库，仅执行正式迁移和种子命令完成初始化，结果为：

- 行测题目：当前数据库共 12224 道可用题，其中资料分析 1035 道、完整材料 207 份；无完整材料的历史资料分析题均已停用。
- 申论材料：3 套。
- 申论题目：6 道。
- 演示用户：2 个。
- 迁移状态：最新。

演练完成后临时数据库已删除。当前正式服务运行于 `http://localhost:3000`，首页状态为 200，服务与数据库状态均为正常。
