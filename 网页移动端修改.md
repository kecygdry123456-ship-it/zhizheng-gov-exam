# 网页移动端整改指南

> 交付对象：负责网页端移动适配的后续 Agent  
> 审计日期：2026-07-11  
> 当前技术栈：Next.js 16、React 19、TypeScript、Tailwind CSS 4  
> 目标：网页将作为安卓 App（WebView 壳）的主要用户界面。整改后，手机网页与桌面网页应具备一致的用户功能，且在 Android Chrome/WebView 中可完整操作。

## 一、范围约束

本任务只处理网页端移动适配、移动交互、可访问性和相应测试，不改变产品业务方向。

允许修改：

- `src/components/app/` 下与响应式布局、移动导航、表单、题卡、资料展示、状态反馈有关的组件。
- `src/app/globals.css` 中的响应式、触控、安全区、动态视口和无障碍样式。
- `src/app/layout.tsx` 中与移动 viewport、主题色有关的声明。
- `tests/ui.smoke.test.mjs`、`TEST_CASES.md` 中的移动端用例。
- 为复用移动抽屉、底部操作栏、确认对话框、网络状态提示而新增网页组件。

除非另有需求，不应修改：

- 后端 API 路径、请求/响应结构、Prisma 模型、迁移和题库数据。
- 登录 Cookie 机制及服务端权限边界。
- 桌面端已有功能、深蓝与橙色的品牌视觉风格。
- 安卓工程、APK 构建配置或原生代码。

兼容性原则：

- 保持现有相对路径 API，例如 `/api/auth/login`、`/api/questions/session`，不要在网页中硬编码开发机、局域网或云服务器域名。这样部署到云服务器后可继续使用同源 Cookie 会话。
- 移动端不能通过“隐藏入口”规避学员功能。学员的登录、退出、首页、专项练习、模拟考试、申论训练、智能规划、错题与收藏、学习分析必须均可操作。
- 管理端是否进入 APK 由主任务最终决定：若保留管理员入口，必须完成本文标注的管理员移动适配；若明确不纳入移动范围，应在移动端隐藏入口并提示使用桌面后台，不能保留不可用的半成品入口。
- 每完成一个优先级，先补测试并执行自检，再进入下一级；不得只靠肉眼判断“看起来没问题”。

## 二、优先级定义

- **P0（发布阻断）**：导致手机端核心功能不可达、无法退出、考试关键操作不可用或移动管理员核心操作不可用。全部修复后才可交付 APK。
- **P1（必须修复）**：明显影响导航、输入、资料阅读、弱网可靠性、触控或无障碍，可能造成误操作、数据未保存或部分用户无法使用。
- **P2（体验优化）**：不阻断主流程，但影响信息密度、可读性、反馈一致性和触摸端观感。

## 三、P0 问题

### P0-01 移动端缺少退出登录入口

- 涉及文件：`src/components/app/app-shell.tsx`，重点检查移动头部、移动菜单和桌面顶栏。
- 现象：用户名、角色和“退出”仅存在于被 `hide-mobile` 隐藏的桌面顶栏；移动菜单只渲染页面导航。手机用户登录后无法正常退出，会造成账号安全和共享设备使用问题。
- 建议：在移动抽屉底部显示当前用户、角色/备考目标和退出按钮；退出期间禁用按钮并显示进度；成功后关闭抽屉并返回登录页。不要复制一套新的认证逻辑，应继续调用现有 `onLogout`。
- 验收：390×844 下，登录后两次点击内可完成退出；退出后页面回到登录页，刷新不恢复旧会话，`GET /api/auth/me` 返回 401。

### P0-02 移动菜单在矮屏和横屏下内容不可达

- 涉及文件：`src/components/app/app-shell.tsx`、`src/app/globals.css`。
- 现象：菜单固定在 `top-[68px]`，学员有 7 个、管理员有 8 个导航项，没有最大高度和纵向滚动。844×390 横屏、低高度设备或字体放大时，底部菜单项和新增的退出按钮会超出视口。
- 建议：不要继续依赖写死的 68px。移动导航应使用实际头部高度、`100dvh` 和 safe-area 计算可用高度，设置 `overflow-y:auto`、底部安全区和必要的滚动阴影。
- 验收：320×568、360×640、390×844、844×390 下，所有导航项和退出按钮均可滚动到达；页面根节点没有非预期横向滚动。

### P0-03 考试页头在窄屏下可能挤压关键状态

- 涉及文件：`src/components/app/exam-view.tsx` 中“模拟考试进行中”页头。
- 现象：标题说明与“倒计时 + 放弃”固定横向排列。320/360px、系统字体 125% 或长文案下，倒计时和按钮可能被压缩、换行失控或移出可视区。
- 建议：小屏改为纵向页头；倒计时使用不会被内容挤出的 sticky 状态条；“放弃”作为明确的次要危险操作，不能遮住题干或主操作。
- 验收：320、360、390px 宽且字体 125% 时，标题、已答数量、倒计时和放弃操作均完整可见，无重叠、裁切或横向溢出。

### P0-04 移动考试的题卡和交卷操作距离当前题目过远

- 涉及文件：`src/components/app/exam-view.tsx`、`src/app/globals.css`。
- 现象：≤900px 后两栏直接折为单栏，答题导航被放到题目卡下方。50～100 题时题卡形成很长区域，“提前交卷”也在底部，用户必须大量滚动才能切题或交卷。
- 建议：移动端将题卡改为底部抽屉或全屏弹层；提供固定/粘性底部操作栏，包含“上一题、下一题/交卷、题卡”。顶部持续显示倒计时和保存状态。桌面侧栏保持原样。
- 验收：任何题目位置都无需滚到整页底部即可前后切题、打开题卡和交卷；题卡清晰区分当前、已答、未答；100 题时仍能流畅操作。

### P0-05 管理员移动题库不可用（仅在 APK 保留管理员时适用）

- 涉及文件：`src/components/app/admin-view.tsx`。
- 现象：题库表格设置 `min-w-[860px]`，手机只能横向拖动，摘要、状态、更新时间和操作无法形成清晰对应；编辑表单是 `92vh` 居中长弹窗，软键盘打开后标题、错误或保存按钮可能不可达。
- 建议：移动端改为卡片列表，展示摘要、分类、难度、状态、更新时间和操作；编辑改为全屏页面或全屏 Sheet，顶部固定标题/关闭，底部固定取消/保存，并处理键盘 inset。桌面端仍使用表格与原布局。
- 验收：360px 下无需页面级横向滚动即可搜索、编辑、发布/停用；键盘打开时当前字段、错误提示和保存按钮均可达；关闭后滚动和焦点恢复。

## 四、P1 问题

### P1-01 移动导航不是完整抽屉交互

- 涉及文件：`src/components/app/app-shell.tsx`。
- 现象：缺少遮罩、点击外部关闭、Esc/系统返回关闭、背景滚动锁和焦点管理；移动菜单项没有桌面端的活动态。
- 建议：实现标准抽屉：遮罩、`aria-expanded`、`aria-controls`、当前项 `aria-current="page"`；打开时锁定背景滚动并把焦点移入，关闭后恢复到菜单按钮；点击遮罩、导航项、Esc 或 Android 返回键时关闭。
- 验收：菜单打开时背景不滚动；当前页面有明确高亮；键盘焦点不会进入遮罩后的页面；所有关闭方式有效。

### P1-02 移动顶栏不粘滞且未处理系统安全区

- 涉及文件：`src/components/app/app-shell.tsx`、`src/app/globals.css`、必要时 `src/app/layout.tsx`。
- 现象：移动头部处于普通文档流。长材料、申论长文和题目页面滚动后，导航入口离开视口；未显式处理刘海、挖孔屏和底部手势区。
- 建议：头部使用 `position:sticky` 和合理层级；通过 `env(safe-area-inset-top/bottom)`、`100dvh` 处理系统栏；菜单起始位置与真实头部高度联动。补充适合 WebView 的 viewport/theme color 声明，但不要禁用用户缩放。
- 验收：长页面滚动时顶栏可用且不遮挡首行；刘海屏顶部、底部手势区无内容裁切。

### P1-03 专项练习题号导航在手机上形成超长页面

- 涉及文件：`src/components/app/practice-view.tsx`。
- 现象：桌面右侧题号导航在手机变为题目下方，最多 100 个题号按钮会显著拉长页面，频繁切题成本高。
- 建议：与考试页复用移动题卡抽屉和底部操作栏；页面只常驻当前进度、提交/下一题和前后题操作。
- 验收：100 题训练时不常驻展示整块长题卡；三次操作内可跳转到任意题号；提交、下一题和完成练习始终可达。

### P1-04 错题与收藏卡片在窄屏下信息被压缩

- 涉及文件：`src/components/app/wrong-view.tsx`。
- 现象：图标、正文和按钮固定一行，题干使用单行 `truncate`。手机上只能读到很短的题干，按钮容易挤压正文。
- 建议：≤560px 改为上下布局或整卡可点；题干至少显示 2～3 行；操作按钮换到下一行并达到 44px 点击高度；明确显示“错题、收藏或两者”标签。
- 验收：360px 下能读到至少两行题干，按钮不溢出，长文本不撑破卡片。

### P1-05 “重新练习”文案与实际行为不一致

- 涉及文件：`src/components/app/wrong-view.tsx`，必要时涉及 `exam-app.tsx` 和练习状态传递。
- 现象：按钮仅调用 `onView("practice")`，进入普通专项题组，并不保证出现用户点击的题目。
- 建议：如果暂不实现单题复练，文案改为“去专项练习”；如保留“重新练习”，必须传递 `questionId` 或错题模式并加载对应题目。
- 验收：按钮文案与跳转结果一致；若为复练，首题必须是所点题目，并可完成提交与结果保存。

### P1-06 资料图片默认强制最小宽 520px

- 涉及文件：`src/components/app/material-view.tsx`。
- 现象：图片使用 `min-w-[520px]`。虽然外层可横向滚动，但手机阅读需要反复左右拖动，且容易被误认为整页溢出。
- 建议：默认图片适应内容宽度；提供“查看原图/放大”灯箱，允许双指缩放；仅在原图模式保留大尺寸，并在需要横向移动时给出提示。
- 验收：默认状态页面不横向滚动；图片可放大到清晰阅读；关闭原图后恢复原页面滚动位置。

### P1-07 资料表格缺少移动阅读辅助

- 涉及文件：`src/components/app/material-view.tsx`。
- 现象：宽表格只有 `overflow-x-auto`，没有横向滑动提示、清晰表头/首列或全屏查看方式，用户难以对应行列。
- 建议：局部横向滚动并给出提示；强化表头/首列；复杂表格提供全屏查看。触摸表格时不能带动整页横移。
- 验收：360px 下宽表只在自身容器滚动，页面根节点不滚动；用户能辨识表头和当前行列。

### P1-08 多个页头在窄屏下仍强制横向排列

- 涉及文件：`src/components/app/study-plan-view.tsx`、`src/components/app/admin-view.tsx`、`src/components/app/exam-view.tsx`。
- 现象：“标题 + 调整设置/新增题目/考试操作”使用 `justify-between` 横排，小屏或字体放大时标题被挤窄。
- 建议：抽取统一响应式 PageHeader；小屏纵向堆叠，操作按钮可满宽或放入固定操作区，标题和说明允许自然换行。
- 验收：320px、字体 125% 下页头无重叠，文字完整，按钮不超出容器。

### P1-09 触控目标普遍小于 44×44 CSS px

- 涉及文件：`src/components/app/app-shell.tsx`、`practice-view.tsx`、`exam-view.tsx`、`admin-view.tsx`、`src/app/globals.css`。
- 现象：汉堡按钮、收藏星、h-8/h-9 题号、纯文字“编辑/停用”等命中区域偏小。
- 建议：图标、题号和关键操作最小命中框 44×44；视觉图标可以较小，但通过 padding 扩展点击区；补充 `focus-visible` 和准确的 `aria-label`/选中态。
- 验收：关键控件命中框均不小于 44×44；TalkBack 可读出名称、当前/选中态和禁用态。

### P1-10 生产登录页公开并预填演示账号密码

- 涉及文件：`src/components/app/login-view.tsx`。
- 现象：默认填入学员账号与固定密码，并在页面公开管理员账号和密码。云端正式环境和 APK 会直接暴露这些信息。
- 建议：只在明确的开发/演示配置下显示演示凭据；生产环境输入框默认空；设置正确的邮箱/密码 `autocomplete`；任何环境都不应公开正式管理员密码。
- 验收：生产构建不显示固定账号和密码，输入框为空；开发演示信息只能通过显式开关启用。

### P1-11 登录、申论和长表单未充分处理软键盘

- 涉及文件：`src/components/app/login-view.tsx`、`essay-view.tsx`、`study-plan-view.tsx`、`admin-view.tsx`、`src/app/globals.css`。
- 现象：大量使用 `min-h-screen`、大间距和长表单；Android 软键盘弹出后，当前字段、申论提交或计划生成按钮可能离开可视区。
- 建议：使用 `100dvh`；表单容器允许纵向滚动；输入聚焦时滚入可视区；长文本页用不遮挡输入内容的 sticky footer，并考虑底部安全区。
- 验收：键盘打开时当前输入框可见，能够滚动到提交按钮；关闭键盘后布局不跳动或错位。

### P1-12 响应式规则过少且全局隐藏横向溢出会掩盖缺陷

- 涉及文件：`src/app/globals.css`。
- 现象：当前主要只有 900px、560px 两档，并使用 `html, body { overflow-x:hidden; }`。超宽组件可能被直接裁切，现有“scrollWidth 不超过 clientWidth”测试因此可能假通过。
- 建议：增加 320/360 小屏、横屏、平板、字体放大、safe-area、dvh 的组件级策略；先修复具体组件宽度，再谨慎决定根节点是否使用 `overflow-x:clip`。表格和材料只能在明确的局部容器滚动。
- 验收：临时移除/放宽根节点隐藏溢出后，页面仍没有非预期横向滚动；所有超宽内容可在局部容器访问。

### P1-13 缺少统一弱网、离线和答案保存状态

- 涉及文件：`src/components/exam-app.tsx`、`practice-view.tsx`、`exam-view.tsx` 及其他发起写请求的组件。
- 现象：多数请求只显示局部错误文本。移动网络切换时，用户可能继续答题却误以为答案已保存；交卷前也没有集中提示未同步答案。
- 建议：增加统一网络状态条；考试答案显示“保存中/已保存/保存失败”；失败可重试，交卷前校验未同步答案。不得仅在前端盲目重复提交导致重复记录，必要的幂等改造需先与后端 Agent 协调。
- 验收：断网选择答案时明确显示未保存；恢复网络后可手动或自动补传；不会重复生成作答或交卷记录。

## 五、P2 问题

### P2-01 练习与考试设置区在手机首屏信息过密

- 涉及文件：`src/components/app/practice-view.tsx`、`exam-view.tsx`。
- 现象：难度档位、推荐、自定义、快捷题量、数字输入和难度下拉同时常驻，手机首屏几乎只显示配置。
- 建议：已有配置时先展示摘要与“调整设置”；高级配置放入折叠区或底部 Sheet；题量快捷按钮和数字输入避免重复占据主要空间。
- 验收：已配置用户进入页面时首屏能看到开始入口或当前题；展开设置后仍可完成全部配置且收起不丢值。

### P2-02 统计趋势图字号过小且缺少无障碍替代

- 涉及文件：`src/components/app/statistics-view.tsx`。
- 现象：七天趋势图日期和数值为 10px，柱形主要依赖视觉表达，TalkBack 难以理解。
- 建议：字号至少 12px；保证 7 列稳定显示或在图表内部滚动；为每日柱形增加 `aria-label`，或提供隐藏数据表/文字摘要。
- 验收：360px 下日期与数值可辨认、不重叠；TalkBack 能逐日读出日期和题量。

### P2-03 原生 `confirm()` 与移动交互不一致

- 涉及文件：`src/components/app/exam-view.tsx`、`admin-view.tsx`。
- 现象：放弃考试、停用题目使用浏览器原生确认框，无法统一焦点、返回键和加载状态。
- 建议：改为统一确认 Dialog，说明操作后果，区分危险主按钮和取消按钮；确认期间防重复。
- 验收：焦点锁定在对话框内；Android 返回键先关闭对话框；取消不改变数据；确认中不可重复触发。

### P2-04 全局错误提示未适配窄屏和底部安全区

- 涉及文件：`src/components/exam-app.tsx` 中全局/未登录错误提示。
- 现象：固定底部提示按内容自然宽度展示，缺少最大宽度、关闭操作和 safe-area；长错误可能贴边或遮住底部操作栏。
- 建议：设置 `max-width:calc(100vw - 32px)`、自动换行和底部安全区；支持关闭或合理超时，使用适当的 live region 供 TalkBack 播报。
- 验收：320px 下长错误完整可读、不横溢、不遮住主按钮，TalkBack 可播报。

### P2-05 鼠标 hover 位移不应直接作用于触屏

- 涉及文件：`src/app/globals.css` 中 `.btn-primary:hover`。
- 现象：部分触摸浏览器可能出现粘滞 hover 或按钮视觉跳动。
- 建议：hover 只在 `@media (hover:hover) and (pointer:fine)` 生效；触屏使用稳定的 active 状态。
- 验收：触屏点击后不残留 hover、不产生布局抖动；鼠标设备仍有反馈。

## 六、建议实施顺序

1. 先补充能复现 P0 的自动化用例，确认测试在旧实现上确实失败。
2. 重构移动 AppShell：安全区头部、可滚动抽屉、活动态、退出入口、焦点和返回行为。
3. 完成考试页移动操作栏、题卡抽屉、倒计时和保存状态，这是 APK 可用性的核心。
4. 复用考试题卡方案处理专项练习。
5. 处理错题卡、资料图片/表格、统一页头、44px 触控目标和软键盘。
6. 根据管理员是否进入 APK，完成移动管理端或明确隐藏入口。
7. 加入弱网/离线反馈并与后端确认幂等边界。
8. 完成 P2、全量回归和 Android Chrome/WebView 真机验收。

## 七、自动化验收用例

应扩充 `tests/ui.smoke.test.mjs`。现有 `UI-003` 只在 390×844 打开一次专项练习并检查根节点 `scrollWidth`，覆盖不足，而且会被全局 `overflow-x:hidden` 掩盖。至少新增以下用例：

### MW-P0-001 移动退出闭环

1. 390×844 登录学员。
2. 打开移动菜单，校验用户信息和退出按钮可见。
3. 点击退出并等待登录页。
4. 刷新页面并请求 `/api/auth/me`。

预期：退出按钮有进行中状态；最终会话为 401，旧页面不可恢复。

### MW-P0-002 矮屏/横屏菜单可达

分别以 320×568、360×640、844×390 登录并打开菜单，滚动到最后一项和退出按钮。

预期：所有项目可点击；菜单局部纵向滚动；背景不滚动；页面无横向溢出。

### MW-P0-003 窄屏考试页头与关键操作

以 320×568 和 360×800 进入考试，模拟 125% 字体或等效浏览器缩放。

预期：倒计时、放弃、前后题、题卡和交卷均完整可见，无重叠。

### MW-P0-004 大题量移动题卡

生成 100 题考试和 100 题专项训练，打开题卡并跳转到末题。

预期：三次操作内跳转成功；题卡不作为超长常驻内容；当前/已答/未答状态正确。

### MW-P0-005 管理端移动闭环（条件用例）

仅在 APK 保留管理员时执行：360×800 登录管理员，搜索题目、打开编辑、修改并保存，再执行停用确认。

预期：全流程无需页面横向滚动；软键盘下保存按钮可达；数据结果与桌面端一致。

### MW-P1-001 抽屉可访问性

检查菜单按钮 `aria-expanded`、菜单关联、当前项 `aria-current`；通过点击遮罩、Esc 和返回行为关闭菜单。

预期：背景滚动被锁定；焦点进入菜单并在关闭后回到触发按钮。

### MW-P1-002 资料图片与宽表格

在 360×800 打开包含图片和宽表格的资料分析题。

预期：根页面不横向滚动；图片默认适应宽度并可放大；表格只在局部容器滚动。

### MW-P1-003 软键盘表单

在登录、申论、智能规划和管理员表单中逐个聚焦底部字段。

预期：字段、错误和提交按钮可滚动到达；键盘关闭后布局恢复。

### MW-P1-004 弱网与答案保存

进入考试后切换离线，选择答案，再恢复网络并重试/等待补传。

预期：离线时明确显示“未保存”；恢复后显示“已保存”；交卷结果只产生一份记录。

### MW-P1-005 生产登录信息保护

以生产配置启动页面。

预期：输入框为空；页面源码和可见文本均不含固定学员/管理员密码。

## 八、人工与真机验收矩阵

必须覆盖：

- 视口：320×568、360×800、390×844、412×915、844×390、768×1024。
- 字体：默认与 125%。
- 环境：桌面 Edge 设备模拟、Android Chrome 真机、最终 APK 内 WebView。
- 页面：首页、专项练习、资料分析题、模拟考试、申论长文本、智能规划长表单、错题与收藏、学习分析；若保留管理员，再覆盖题库列表和编辑。
- 系统行为：顶部刘海/挖孔、底部手势区、软键盘、Android 返回键、网络断开与恢复、屏幕旋转。

统一通过标准：

- 不存在非预期的页面级横向滚动或被裁切内容。
- 所有核心按钮可达，关键触控目标不小于 44×44 CSS px。
- 手机端与桌面端学员功能一致，操作结果写入同一后端数据。
- 菜单、弹层、键盘和返回键行为明确，无双重滚动或焦点丢失。
- 考试倒计时、答案保存、题卡和交卷在长题量下仍可用。
- TalkBack 可读出主要控件名称、状态和错误。

## 九、自检命令与交付要求

每轮修改至少执行：

```bash
npm run lint
npx prisma validate
npm run build
npm run test:ui
```

如果完整数据库环境可用，再执行：

```bash
npm test
```

交付报告应列出：修改文件、对应问题编号、新增测试、命令结果、未完成项、真机型号/Android 版本/WebView 版本。不得把仅在桌面设备模拟器通过描述为“真机验收通过”。

## 十、禁止事项

- 禁止为消除测试中的横向滚动，简单给 `html/body` 或大容器继续叠加 `overflow:hidden`，从而裁掉不可达内容。
- 禁止通过删除、隐藏学员功能入口来宣称移动端适配完成。
- 禁止在网页代码中硬编码 `localhost`、`10.0.2.2`、局域网 IP、测试域名、云服务器 IP 或 API Key。
- 禁止将登录令牌写入 `localStorage`，也不要绕过现有 HttpOnly Cookie 会话。
- 禁止在生产页面展示演示密码、管理员密码或完整会话值。
- 禁止擅自修改后端接口结构、数据库模型和业务判题逻辑；若弱网幂等确需后端支持，先形成接口需求交给后端 Agent。
- 禁止只为手机重写另一套业务页面，造成桌面/手机逻辑分叉；应复用同一数据与业务组件，通过响应式展示解决布局差异。
- 禁止禁用页面缩放或限制用户放大资料图片，这会破坏可访问性。
- 禁止只测试 390×844 单一视口，或只用 `document.scrollWidth` 判断移动适配完成。
- 禁止在未执行 lint、build 和对应 UI 用例时标记问题已完成。
# 2026-07-11 全量复验新增问题

执行 `npm run verify` 时，网页生产构建、Prisma 校验和全部 API 集成测试通过；39 项自动化测试中 37 项通过，以下 2 项失败。Android 开发任务不直接修改这些网页问题。

1. `UI-011：访客可注册学员账号、退出并使用新账号重新登录`
   - 失败位置：`tests/ui.smoke.test.mjs:74`，辅助函数断言登录页应存在切换到注册模式的按钮。
   - 现象：测试未定位到注册模式入口。
   - 建议：核对登录页注册入口的可访问名称、渲染条件和测试约定；不要只用视觉图标，确保按钮有稳定文本或 `aria-label`。

2. `UI-001：学员可完成登录、练习、统计查看和退出`
   - 失败位置：`tests/ui.smoke.test.mjs:117`。
   - 现象：`getByRole('button', { name: '退出' })` 同时命中侧栏图标按钮和顶部文字按钮，Playwright 严格模式报错。
   - 建议：为不同位置提供不冲突的可访问名称，例如“退出登录”和“侧栏退出登录”，或让测试根据明确容器定位；同时确认移动端只保留一个可达且语义明确的退出入口。
