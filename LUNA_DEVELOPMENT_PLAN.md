# GPT-5.6-luna 继续开发提示词 Plan

> 用途：将本文档中的“主提示词”完整复制给 GPT-5.6-luna，让它在当前项目目录中继续开发知政公考系统。  
> 项目目录：`C:\Users\Administrator\Documents\New project 2`  
> 配套文档：`README.md`、`ITERATION_PLAN.md`、`prisma/schema.prisma`

## 一、使用方法

推荐按以下方式使用：

1. 让 GPT-5.6-luna 打开当前项目目录。
2. 将第二部分的“主提示词”一次性完整发送给它。
3. 要求它先检查工程，再创建执行计划，不要仅根据本文档假设代码现状。
4. 首轮只执行 Sprint 1，不要让它一次性完成全部五个 Sprint。
5. Sprint 1 验收通过后，再依次发送本文档后面的阶段提示词。
6. 每轮都要求它实际修改代码、运行检查并汇报剩余问题。

如果你所使用的平台不识别 `GPT-5.6-luna` 这个模型名称，请在平台中选择实际可用的对应模型，但不要改变以下任务目标和验收标准。

---

## 二、可直接复制的主提示词

```text
你是本项目的主开发工程师，负责继续开发“知政公考”Web 系统。请在现有工程上工作，不要重新创建一个无关项目，也不要丢弃已经完成的界面、数据库模型和用户数据。

# 沟通语言

默认使用简体中文输出进度更新、技术说明、代码交付说明和最终报告。代码标识符、命令和第三方技术名称保留原文。

# 工作目录

C:\Users\Administrator\Documents\New project 2

# 开始前必须阅读

请完整阅读并理解以下文件，然后再决定修改方案：

1. AGENTS.md（如果存在）
2. README.md
3. ITERATION_PLAN.md
4. package.json
5. prisma/schema.prisma
6. prisma/seed.ts
7. src/components/exam-app.tsx
8. src/app/api 下的全部现有接口
9. src/lib/session.ts
10. src/lib/prisma.ts

同时检查：

- Git 当前状态和已有未提交改动。
- Node.js、npm、PostgreSQL 和 Prisma 是否可用。
- 当前数据库是否运行，现有数据表和种子数据是否存在。
- npm run lint、npm run build 当前是否通过。
- 当前页面是否可启动，以及登录接口是否正常响应。

不要覆盖不属于本轮任务的用户改动。修改数据模型前，先理解现有数据关系和可能受到影响的代码。

# 项目现状

当前工程使用：

- Next.js App Router
- React
- TypeScript
- Tailwind CSS
- Prisma
- PostgreSQL
- Zod
- jose
- bcryptjs

当前已有：

- 登录页和登录接口。
- 学习首页、专项练习、模拟考试、错题收藏、学习分析和题库管理界面。
- User、Category、Question、ExamPaper、Attempt、Favorite 等数据模型。
- 题目、作答记录和收藏基础接口。
- 学员与管理员种子账号。

当前主要问题：

- 页面主体集中在 src/components/exam-app.tsx，组件过大。
- 前端题目仍以内置数组为主。
- 作答记录、收藏和统计仍有浏览器本地实现。
- 学员读取题目时可能提前获得正确答案和解析。
- 管理端尚未形成完整的新增、编辑、发布和停用流程。
- 模拟考试仍是演示流程。
- 缺少真实统计、退出登录、当前用户接口和完整页面访问控制。
- 缺少关键业务自动化测试。

# 总目标

把当前“交互演示首版”迭代为“真实可用首版”。

真实可用首版必须满足：

1. 用户登录后，从 PostgreSQL 获取真实题库。
2. 作答、收藏、错题和学习统计全部以数据库数据为准。
3. 刷新页面或更换浏览器后，登录用户仍能看到一致数据。
4. 学员提交答案前，普通题目响应不返回正确答案和完整解析。
5. 管理员能够新增、编辑、发布和停用题目。
6. 页面按业务路由和组件拆分，不再把全部功能放在一个文件中。
7. 服务端接口具备统一输入校验、错误响应和角色判断。
8. 桌面端和移动端都能完成登录、练习、收藏、错题复习和查看统计。
9. lint、类型检查、生产构建和关键流程测试全部通过。
10. README 和 ITERATION_PLAN.md 根据实际实现同步更新。

# 当前执行范围

本轮只完成 Sprint 1：真实练习闭环。不要在 Sprint 1 未完成时扩展申论、付费、消息通知或复杂运营功能。

Sprint 1 包含：

1. 拆分登录、应用布局、学习首页和专项练习模块。
2. 增加当前用户接口与退出登录接口。
3. 建立服务端页面访问判断：未登录不能进入学员页面，非管理员不能进入管理页面。
4. 分类和题目改为从数据库读取。
5. 调整题目接口，提交答案前不返回 answer 和 explanation。
6. 增加独立答题提交接口，由服务端判断结果并写入 Attempt。
7. 收藏按钮接入 Favorite 数据表。
8. 错题列表根据 Attempt 数据生成，不再依赖浏览器本地记录。
9. 首页基础指标改为读取真实统计，不再显示固定演示数值。
10. 增加必要的加载、空数据、失败、重试和提交中状态。
11. 为登录、获取题目、提交答案、收藏和错题查询增加测试。
12. 更新项目文档。

# 架构要求

优先采用清晰、可维护的结构。可以根据工程实际情况微调，但至少应形成以下职责边界：

- src/app/(auth)：登录相关页面。
- src/app/(student)：学员页面和共享布局。
- src/app/admin：管理页面。
- src/components/ui：按钮、输入框、弹窗、空状态等通用组件。
- src/components/layout：侧边栏、顶部栏和移动端导航。
- src/features/auth：用户会话相关组件、类型和请求。
- src/features/practice：练习页面、题目卡片、答题状态和请求。
- src/features/favorites：收藏业务。
- src/features/statistics：统计展示和请求。
- src/lib/validation：Zod 输入规则。
- src/lib/services：数据库业务服务。

页面组件负责组合和展示；数据库查询、评分、统计等业务逻辑不要堆积在 React 页面中；服务端接口保持精简，并调用可测试的业务服务。

# 数据与接口要求

统一成功响应建议使用：

{
  "data": ..., 
  "meta": ...
}

统一错误响应建议使用：

{
  "error": {
    "code": "STABLE_ERROR_CODE",
    "message": "适合用户理解的中文说明",
    "details": null
  }
}

分页响应必须包含：items、page、pageSize、total、totalPages。

至少实现或完善以下接口：

- GET /api/auth/me
- POST /api/auth/logout
- GET /api/categories
- GET /api/questions
- GET /api/questions/:id
- POST /api/questions/:id/answer
- GET /api/favorites
- POST /api/favorites
- DELETE /api/favorites/:questionId
- GET /api/wrong-questions
- GET /api/statistics/overview

题目列表和详情在提交前只返回：

- id
- category
- type
- stem
- options
- difficulty
- 必要的展示字段

不返回 answer 和 explanation。提交答案后，由服务端返回：

- correct
- selected
- correctAnswer
- explanation
- attemptId

服务端必须根据数据库中的答案计算是否正确，不能信任前端传入的正确性结果。

# 数据库要求

检查现有模型是否足够支持 Sprint 1。只有确有必要时才修改模型。

如果修改模型：

1. 使用 Prisma Migration 生成迁移文件。
2. 不使用仅同步结构但不保留迁移记录的方式作为最终交付。
3. 保留现有演示账号和可用题目。
4. 更新 seed，使全新数据库可以一次初始化成功。
5. 为高频查询字段增加合理索引。

可以为错题状态增加独立模型，也可以在 Sprint 1 根据 Attempt 聚合，只要结果准确、查询清晰且后续可扩展。请说明你的选择和理由。

# 登录和角色要求

- 使用现有 HttpOnly 会话 Cookie 机制继续完善。
- 增加当前用户读取和退出登录。
- 登录失败使用统一错误结构。
- 页面首次加载时不应要求用户每次重复登录；有效会话应自动恢复。
- 学员不能进入管理员页面。
- 管理员可以进入题库管理页面。
- 会话过期时，页面应跳转登录页并显示明确提示。
- 不在日志或页面中输出完整会话值和用户密码。

# 前端体验要求

保留当前“知政公考”的深蓝与橙色视觉风格，不要改成通用模板页面。

必须处理：

- 桌面侧边栏。
- 移动端导航。
- 页面加载骨架或加载提示。
- 接口失败提示和重试按钮。
- 空题库、无错题和无收藏状态。
- 提交答案期间按钮不可重复点击。
- 收藏操作的即时视觉反馈。
- 未登录、无管理权限和会话过期状态。
- 小屏幕下题目选项、表格和导航不溢出。

不要为了视觉效果增加与学习任务无关的大面积装饰。优先保证首屏能看到学习目标、今日数据和主要操作。

# 测试与验证

完成修改后，必须运行与本轮相关的检查。至少包括：

1. npm run lint
2. TypeScript 检查；如果项目没有独立脚本，使用生产构建中的类型检查或补充 typecheck 脚本。
3. npm run build
4. Prisma schema 校验。
5. 数据库迁移状态检查。
6. 种子脚本在可控环境中的验证。
7. 核心接口测试。
8. 最小浏览器流程测试：登录 → 获取题目 → 提交答案 → 收藏 → 查看错题 → 查看统计 → 退出登录。

如果项目目前没有测试框架，请选择与 Next.js 和 TypeScript 兼容、配置简单的方案，并只增加本轮所需测试。不要为了测试框架进行大范围无关改造。

验证失败时，先定位并修复本轮代码引起的问题，再重新运行。不要把仍然失败的构建描述成完成。

# 工作方式

这是一个需要实际修改工程的开发任务，不是只输出建议。

开始时：

1. 用一到两句话说明你正在检查什么。
2. 阅读代码和文档。
3. 给出一个简短、可追踪的执行计划。
4. 标记当前正在进行的步骤。

执行中：

- 优先完成可以端到端验证的业务切片。
- 每完成一个关键阶段，简短报告已完成内容和下一步。
- 发现文档与代码不一致时，以实际代码为依据，并在最终报告中说明。
- 对不会改变产品方向的小问题自行做合理决定。
- 只有当缺失信息会明显改变数据结构、用户体验或造成不可接受的返工时，才向用户提问。
- 不要只创建空文件、占位按钮或没有接入数据的页面来宣称功能完成。

# 建议执行顺序

按依赖关系推进，允许你根据实际代码调整细节：

阶段 A：基线检查
- 阅读工程和 Git 状态。
- 运行现有检查。
- 确认数据库、演示账号和题目数据。
- 列出 Sprint 1 具体修改文件。

阶段 B：会话和路由
- 完善 session 服务。
- 实现 me 与 logout。
- 建立登录页、学员布局和管理员页面访问判断。
- 验证会话恢复和退出登录。

阶段 C：题库读取和答题
- 建立分类、题目查询服务和输入校验。
- 调整题目响应字段。
- 实现服务端答题判断与 Attempt 写入。
- 将练习页面接入真实接口。

阶段 D：收藏、错题和统计
- 收藏全部改为数据库数据。
- 实现错题查询。
- 实现首页真实基础统计。
- 移除对应 localStorage 业务数据和固定统计值。

阶段 E：组件拆分和体验补齐
- 拆分大型组件。
- 增加通用加载、错误和空状态。
- 检查桌面端和移动端布局。

阶段 F：测试、文档和最终检查
- 增加测试。
- 执行所有检查和浏览器流程。
- 更新 README.md 和 ITERATION_PLAN.md。
- 输出最终交付报告。

# 成功标准

只有同时满足以下条件，Sprint 1 才能标记为完成：

- 所有计划内功能都有真实代码和数据支持。
- 练习题从数据库读取。
- 正确答案在提交前不出现在学员题目响应中。
- 作答、收藏、错题和首页统计均来自 PostgreSQL。
- 同一账号重新登录后数据保持一致。
- 登录状态可以自动恢复，退出登录有效。
- 学员和管理员页面边界正确。
- 大型 exam-app.tsx 已实质拆分，不再承担全部页面职责。
- 关键接口和核心用户流程存在测试。
- lint、类型检查、生产构建和数据库检查全部通过。
- 文档与实际代码一致。

# 停止规则

- 如果发现数据库无法连接，先检查本地服务、环境变量和 Prisma 配置；完成合理诊断后仍无法恢复，再向用户说明阻塞点和已检查内容。
- 如果现有未提交改动与本轮修改重叠，保留用户改动并谨慎合并；无法可靠判断意图时再询问。
- 如果某项功能需要本轮范围外的第三方配置，先实现清晰的服务边界和可运行的本地替代方案，并在最终报告中标明配置步骤。
- 不要因为部分页面看起来完整就提前结束。应以成功标准和实际验证结果为准。
- 完成 Sprint 1 后停止，不自动开始 Sprint 2。等待用户验收或明确要求继续。

# 最终输出格式

最终报告使用简体中文，先给出结果，再给出必要细节，包含：

1. 完成情况概述。
2. 主要修改文件和对应功能。
3. 数据模型或迁移变化。
4. 已执行的检查及结果。
5. 演示账号和启动方式。
6. 尚未完成或需要用户配置的事项。
7. 建议下一步，但不要自动执行 Sprint 2。

现在开始。先检查项目和当前运行状态，然后给出 Sprint 1 的执行计划并实际实施。
```

---

## 三、Sprint 1 验收后发送的 Sprint 2 提示词

```text
继续开发知政公考系统。本轮只执行 ITERATION_PLAN.md 中的 Sprint 2：真实统计与管理端。

开始前先检查 Sprint 1 的代码、迁移、测试和 Git 状态，确认以下能力仍然正常：登录恢复、退出登录、数据库题库、服务端答题、收藏、错题和真实首页统计。

本轮目标：

1. 建立完整统计服务，支持总览、七天/三十天趋势、模块能力和薄弱题型。
2. 学习首页与学习分析页全部使用真实数据，移除剩余固定数值。
3. 管理端完成题目分页检索、新增、编辑、发布、停用和查看详情。
4. 增加题目来源、年份、地区、考试类型和知识点等运营字段；确有需要时使用 Prisma Migration 调整模型。
5. 增加管理员操作记录。
6. 所有管理写入接口使用统一 Zod 校验、错误结构和角色判断。
7. 增加统计服务、题目管理和角色边界测试。
8. 更新 README.md 和 ITERATION_PLAN.md。

统计口径必须在代码中集中定义，首页和分析页不得各自重复计算。统计时间使用 Asia/Shanghai。正确率的分母、未作答处理、重复作答处理和模块归属必须有明确规则并写入文档。

管理端不能只做静态表单。新增和编辑后必须真实写入数据库；发布状态变化后学员题库应即时反映；已有作答记录的题目优先停用，不直接删除历史关联。

完成后运行 lint、类型检查、生产构建、Prisma 检查、接口测试和管理端浏览器流程测试。全部通过后停止，不自动开始 Sprint 3。
```

## 四、Sprint 2 验收后发送的 Sprint 3 提示词

```text
继续开发知政公考系统。本轮只执行 Sprint 3：完整模拟考试闭环。

目标：

1. 将试卷结构改为可维护的试卷与题目关联模型，保存顺序、分值和所属模块。
2. 管理端支持创建试卷、选题、排序、设置时长、总分、开放时间、发布和下线。
3. 学员开始考试时创建 ExamSession，服务端保存开始时间和截止时间。
4. 每次选择答案自动保存，刷新页面后恢复答案、题目位置和剩余时间。
5. 剩余时间以服务端截止时间为准，到时自动交卷。
6. 支持题目导航、未答提醒和待检查标记。
7. 交卷后生成固定 ExamResult，重复请求不能重复计算成绩。
8. 报告展示总分、模块得分、正确率、耗时、未答题、逐题解析和复习建议。
9. 增加考试时间、答案保存、自动交卷、成绩计算和角色边界测试。

不要用前端固定题目数组或前端单独计算结果作为最终数据来源。关键时间和成绩由服务端确定。使用 Prisma Migration 记录数据结构变化，保留已有练习记录。

完成后验证：创建并发布试卷 → 学员开始考试 → 中途刷新恢复 → 自动保存 → 手动或到时交卷 → 查看固定报告。所有检查通过后停止，不自动开始 Sprint 4。
```

## 五、Sprint 3 验收后发送的 Sprint 4 提示词

```text
继续开发知政公考系统。本轮执行 Sprint 4：申论首版与智能辅导。

开始前检查当前项目是否已经配置可用的 OpenAI API 环境变量。如果没有，不要把密钥写入代码或版本库；先实现服务边界、配置说明和可运行的本地规则化备用结果，并明确告诉用户还需要什么配置。

本轮目标：

1. 建立申论材料、题目、提交记录和批改反馈数据模型。
2. 实现申论材料阅读、重点标记、笔记、计时、字数统计和自动保存。
3. 支持归纳概括、综合分析、提出对策、贯彻执行和文章写作题型。
4. 实现智能批改服务：结构、要点覆盖、表达质量、具体修改建议和示例改写。
5. 保存模型标识、提示模板版本、输出结果、耗时和用量。
6. 将行测智能助教升级为连续问答，支持题目上下文和历史消息。
7. 设置合理的响应长度、超时处理、每日用量记录和备用解析。
8. 管理端支持维护申论材料、题目、评分点和参考答案。
9. 增加相关接口和核心流程测试。

模型输出只能作为学习建议，页面应明确区分官方解析、参考答案和智能建议。不要把模型生成内容直接当成唯一评分依据；保留评分点匹配和基础规则结果，使反馈可解释。

完成后验证完整流程，并更新环境变量示例和配置文档。所有检查通过后停止，不自动开始 Sprint 5。
```

## 六、Sprint 4 验收后发送的 Sprint 5 提示词

```text
继续开发知政公考系统。本轮执行 Sprint 5：学习计划、质量建设和上线准备。

目标：

1. 实现个人中心、目标考试日期、每日题量和可学习时间设置。
2. 根据用户目标、薄弱模块和考试日期生成每周学习计划。
3. 首页今日任务全部由 StudyPlan 和 StudyTask 数据生成。
4. 实现错题复习、专项练习、模拟考试和申论写作任务。
5. 增加站内消息与计划提醒。
6. 补齐单元测试、接口测试和浏览器流程测试。
7. 增加运行日志、服务状态检查、数据库备份说明和恢复演练文档。
8. 为高频查询补充索引，检查分页、缓存和移动端性能。
9. 配置正式 Prisma Migration 工作流，区分开发、测试和正式环境。
10. 完成上线检查清单、环境变量清单和运维说明。

完成标准：新用户可以设置考试目标，获得真实学习计划，按计划完成任务并看到数据变化；管理员可以维护内容；核心流程有自动化测试；生产构建通过；部署和恢复步骤可按文档复现。

完成后输出完整版本报告、剩余产品问题和下一季度建议，不继续扩展新功能。
```

## 七、给 Luna 的代码审查提示词

在每个 Sprint 完成后，可以另开一次任务发送以下提示词，让 Luna 只做检查，不修改代码：

```text
请对当前知政公考工程进行一次只读代码审查，不要修改文件。

重点检查：

1. 当前 Sprint 的成功标准是否真正满足。
2. 页面是否仍存在固定演示数据或未接入的按钮。
3. 学员提交答案前是否能从普通响应中看到正确答案或完整解析。
4. 用户数据查询是否始终限定当前登录用户。
5. 管理接口是否正确判断管理员角色。
6. 输入校验、错误响应、分页和事务是否一致。
7. 考试时间、评分和统计口径是否由服务端统一确定。
8. 数据模型、索引和迁移是否适合当前查询。
9. 组件是否职责清晰，是否存在过大的页面文件或重复业务逻辑。
10. 测试是否覆盖登录、练习、收藏、错题、统计和当前 Sprint 新增流程。
11. README、ITERATION_PLAN.md 与实际代码是否一致。

先运行必要的只读检查和测试，再按严重程度列出问题。每个问题包含文件路径、位置、影响、复现条件和建议修改方向。如果没有发现明确问题，说明已检查的范围和仍然存在的测试盲区。
```

## 八、建议的日常小任务提示词模板

当你不希望 Luna 执行整个 Sprint，只想让它完成一个小功能时，可以使用：

```text
在当前知政公考项目中完成以下任务：

[在这里写一个明确的小任务]

开始前阅读相关代码和 ITERATION_PLAN.md，不要修改无关模块。先说明你找到的现状和准备修改的范围，然后实际实现。

成功标准：

- [可观察结果 1]
- [可观察结果 2]
- [可观察结果 3]

完成后运行与本次修改最相关的测试、lint、类型检查和构建。最终报告包含修改文件、验证结果和剩余事项。若现有实现与任务描述不一致，以代码事实为准并说明差异。
```

## 九、推荐执行节奏

不要一次把 Sprint 1～5 全部交给 Luna。推荐节奏如下：

1. 发送主提示词，只完成 Sprint 1。
2. 人工登录系统，验证练习、收藏、错题和统计。
3. 使用只读代码审查提示词复查 Sprint 1。
4. 修复审查中确认的问题。
5. 提交 Sprint 1 版本。
6. 再发送 Sprint 2 提示词。
7. 后续每个 Sprint 重复“实现 → 人工验收 → 代码审查 → 修复 → 提交”的流程。

这种方式能让每轮目标保持清晰，也方便在产品方向变化时及时调整后续计划。
