# 知政公考 Demo 验收与测试用例

> 测试范围：迭代文档 Sprint 1——真实练习闭环  
> 自动化文件：`tests/api.integration.test.mjs`、`tests/ui.smoke.test.mjs`  
> 执行命令：`npm test`

## 1. 验收目标

本轮验证以下核心结果：

- 注册、登录会话可以创建、恢复和退出。
- 公开注册只能创建学员账号，密码哈希等敏感字段不会返回客户端。
- 学员题目响应在提交前不包含正确答案和完整解析。
- 答题结果由服务端根据数据库内容计算。
- 作答记录、收藏、错题和统计真实写入或读取 PostgreSQL。
- 不同用户只能读取自己的学习数据。
- 学员不能执行管理员题目写入操作。
- 输入边界、错误响应和分页结构清晰可识别。
- 工程通过 Prisma 校验、ESLint、TypeScript 和生产构建。

## 2. 测试环境

| 项目 | 要求 |
| --- | --- |
| Node.js | 当前工程已安装版本 |
| PostgreSQL | 本地服务处于运行状态 |
| 数据库 | `.env` 中的 `DATABASE_URL` 可连接 |
| 初始数据 | 已执行 `npm run db:seed` |
| 学员账号 | `student@zhizheng.local` |
| 管理员账号 | `admin@zhizheng.local` |
| 初始密码 | `Demo123456` |
| 测试端口 | 默认 `3107`，可通过 `TEST_PORT` 修改 |

自动化测试会先执行生产构建，再在独立端口启动服务。接口测试使用端口 3107，浏览器测试使用端口 3108。测试产生的注册账号、关联学习数据、作答记录和题目会在结束后清理；收藏状态会恢复到测试前状态。

## 3. 自动化测试用例

### AUTH-001 未登录读取当前用户

- 前置条件：请求不携带会话 Cookie。
- 操作：请求 `GET /api/auth/me`。
- 预期：状态码为 401；响应包含 `error.code=UNAUTHORIZED`。

### AUTH-002 错误登录信息

- 前置条件：使用存在的学员邮箱和错误密码。
- 操作：请求 `POST /api/auth/login`。
- 预期：状态码为 401；响应符合统一错误结构，包含 `error.code` 和 `error.message`。

### AUTH-003 正常登录与会话恢复

- 操作：使用学员演示账号登录，再请求 `GET /api/auth/me`。
- 预期：登录成功；服务端设置 HttpOnly、SameSite=Lax Cookie；当前用户邮箱和角色正确。

### AUTH-004 退出登录

- 前置条件：学员已登录。
- 操作：请求 `POST /api/auth/logout`，随后使用清空后的 Cookie 请求当前用户。
- 预期：退出接口返回成功；Cookie 过期；当前用户接口返回 401。

### AUTH-005 正常注册与自动会话

- 操作：向 `POST /api/auth/register` 提交有效的姓名、混合大小写邮箱、同时包含字母和数字的密码、相同的确认密码及可选目标考试。
- 预期：状态码为 201；邮箱去除首尾空白并转为小写；响应不包含 `passwordHash`；服务端设置 `HttpOnly`、`SameSite=Lax` Cookie；随后请求 `GET /api/auth/me` 可以恢复该注册用户。

### AUTH-006 重复邮箱

- 前置条件：目标邮箱已经存在，或先使用该邮箱完成一次注册。
- 操作：使用大小写或首尾空白不同但规范化后相同的邮箱再次注册。
- 预期：状态码为 409；响应包含 `error.code=ACCOUNT_EXISTS`；数据库中不会出现重复用户。

### AUTH-007 注册密码强度与长度边界

- 操作：分别提交少于 8 位、仅含字母、仅含数字，以及 UTF-8 编码后超过 bcrypt 72 字节限制的密码。
- 预期：各请求均返回 400 和统一输入错误结构，不创建用户，也不创建会话。

### AUTH-008 注册确认密码不一致

- 操作：提交两个不同的密码字段。
- 预期：状态码为 400；响应指出确认密码不一致；数据库中不创建用户。

### AUTH-009 公开注册角色提升防护

- 操作：注册请求额外携带 `role=ADMIN`。
- 预期：注册成功时账号角色仍为 `STUDENT`；该账号不能访问管理员写接口。

### AUTH-010 新账号退出后重新登录

- 前置条件：已通过注册接口创建测试账号并退出。
- 操作：使用规范化后的邮箱和原密码调用 `POST /api/auth/login`。
- 预期：登录成功并创建新会话；`GET /api/auth/me` 返回同一个学员账号。

### QUESTION-001 题目分页与字段保护

- 操作：请求 `GET /api/questions?page=1&pageSize=3`。
- 预期：返回分页字段；题目包含题干、选项、分类和难度；不包含 `answer` 与 `explanation`。

### QUESTION-002 题目详情字段保护

- 操作：请求 `GET /api/questions/:id`。
- 预期：题目详情不包含 `answer` 与 `explanation`。

### QUESTION-003 非法分页参数

- 操作：传入 `page=not-a-number`。
- 预期：状态码为 400；响应包含 `error.code=INVALID_INPUT`，而不是服务端异常页面。

### ANSWER-001 答案索引超出范围

- 操作：提交大于选项数量的答案索引。
- 预期：状态码为 400；不创建作答记录。

### ANSWER-002 正常答题与记录持久化

- 操作：向 `POST /api/questions/:id` 提交有效选项。
- 预期：响应包含是否正确、正确答案、解析和作答记录编号；`GET /api/attempts` 可以读取该记录。

### QUESTION-004 题库规模与模块覆盖

- 操作：读取全部已发布题目。
- 预期：题目不少于 35 道，至少覆盖言语理解、判断推理、数量关系、资料分析和常识判断五个模块。

### QUESTION-005 千题题库与随机组题

- 操作：请求 `GET /api/questions/session?count=50`。
- 预期：完整已发布题库总量不少于 10000 道；本轮返回 50 道随机题；题目不包含提交前不可见字段；可用来源题包含地区、年份和试卷标题。

### QUESTION-006 难度、板块与题量筛选

- 操作：请求 `GET /api/questions/session?count=12&category=言语理解&minDifficulty=4&maxDifficulty=6`，再请求最低难度高于最高难度的组合。
- 预期：返回 12 道言语理解题，每题难度处于 4～6；响应包含套题难度；非法组合返回 400。

### QUESTION-007 完整资料分析题组

- 操作：请求 10 道资料分析题。
- 预期：返回 2 份不同材料，每份材料正好关联 5 道题；所有题目包含材料文字、表格或图片块；不会混入无公共材料题目。

### QUESTION-008 题库占位与测试数据隔离

- 操作：运行 `npm run db:audit:questions`，并检查正式组题接口。
- 预期：已发布占位题和页面测试题均为 0；纯图片题干只要图片有效就继续发布；损坏图片题保持草稿。

### QUESTION-009 模拟卷细分题型配额

- 操作：国考型与省考型各连续生成 3 套试卷，读取 `subtypeCounts`。
- 预期：每个细分题型的 `selected` 等于 `requested`；题干全卷唯一；资料分析保持完整五题组；不包含占位题。

### PREFERENCE-001 训练配置持久化与推荐难度

- 操作：保存专项板块、题量、难度档位以及模拟题量、时长，再重新读取配置。
- 预期：配置与账号绑定并完整恢复；接口根据最近作答返回推荐难度、薄弱板块和推荐原因。

### ESSAY-001 申论作答

- 操作：读取申论材料并提交一篇符合最低字数的答案。
- 预期：返回分数、评分点覆盖、改进建议和参考答案，提交记录写入数据库。

### PLAN-001 智能学习规划

- 操作：提交目标考试、考试日期、每日分钟、每周天数、当前阶段、重点模块和补充说明，再读取最新计划；另提交 API Key 但不提交模型标识。
- 预期：无 Key 时计划至少包含七项任务；Key 不完整或只有 Base URL 时返回 400；支持用户自己的兼容 Base URL；Key 和 Base URL 不写入数据库。

### FAVORITE-001 收藏与取消收藏

- 操作：收藏目标题目，读取收藏列表，再取消收藏。
- 预期：收藏状态依次为 true 和 false；收藏列表与数据库状态一致。

### WRONG-001 错题生成

- 前置条件：已经对目标题目产生一次错误作答。
- 操作：请求 `GET /api/wrong-questions`。
- 预期：错题列表包含目标题目，并且只返回当前登录用户的数据。

### STATS-001 学习统计

- 前置条件：当前测试已产生作答记录。
- 操作：请求 `GET /api/statistics/overview`。
- 预期：累计数量不少于本轮新增记录；正确率处于 0～100；最近七天数组长度为 7。

### ROLE-001 学员角色边界

- 操作：学员请求 `POST /api/questions`。
- 预期：状态码为 403；响应包含 `error.code=FORBIDDEN`；数据库不新增题目。

## 4. 人工页面验收用例

### UI-001 首次登录

1. 清除浏览器当前站点 Cookie。
2. 打开首页。
3. 输入学员演示账号并登录。
4. 确认进入学习首页，刷新页面后仍保持登录。

预期：登录过程有提交中状态；错误信息清晰；刷新后不重复要求登录。

### UI-002 专项练习

1. 进入专项练习。
2. 选择一个答案并提交。
3. 连续快速点击提交按钮。
4. 查看判定结果和解析。
5. 刷新页面并查看累计答题数。

预期：提交期间按钮不可重复操作；只产生一条记录；提交后才展示正确答案和解析；刷新后统计仍存在。

### UI-003 收藏同步

1. 在专项练习中收藏题目。
2. 进入错题与收藏页面。
3. 刷新浏览器。
4. 取消收藏并再次刷新。

预期：收藏状态在不同页面一致，并在刷新后保持。

### UI-004 错题同步

1. 故意答错一道题。
2. 打开错题与收藏页面。
3. 退出账号后重新登录。

预期：错题可见；重新登录后仍然存在。

### UI-005 移动端布局

1. 将浏览器视口调整到 390×844。
2. 依次打开首页、练习、错题和统计页面。
3. 展开和收起移动导航。

预期：题干、选项和按钮不横向溢出；导航可正常使用；主要按钮可见。

### UI-006 管理员与学员页面边界

1. 使用学员账号进入题库管理入口。
2. 使用管理员账号再次进入。

预期：学员不显示题库管理入口；管理员可以进入管理页面并执行真实写入。

### UI-007 管理员题目表单

1. 使用管理员账号进入题库管理。
2. 点击新增题目。
3. 填写分类、题型、题干、四个选项、正确答案和解析。
4. 保存并在列表中检索题目。
5. 编辑题目后停用。

预期：新增和编辑真实写入数据库；发布题目在学员题库可见；停用后从学员题库移除。

### UI-008 模拟考试

1. 使用学员账号进入模拟考试。
2. 开始考试并至少完成一道题。
3. 点击确认交卷。
4. 查看成绩报告和学习统计。

预期：统一交卷成功；报告显示得分、答对数和已作答数；考试记录计入统计。

### UI-009 专项练习完成状态

1. 进入专项练习并切换到最后一题。
2. 提交答案。
3. 点击完成练习。

预期：进入本轮完成报告，显示答题数量和正确率；不会自动返回第一题。

### UI-016 专项练习已答题锁定

1. 提交第 1 题答案并查看解析。
2. 前往下一题，再返回第 1 题。
3. 检查原选择、解析、选项状态和提交按钮。

预期：原答案与解析完整恢复；所有选项锁定，不再出现提交按钮；本轮答题数、正确数和数据库作答记录均不重复增加。

### UI-010 申论训练与智能规划

1. 进入申论训练并提交作答。
2. 查看评分反馈和参考答案。
3. 进入智能规划，填写每日学习分钟和重点模块并生成计划。
4. 确认页面提供密码类型的 API Key 输入框和模型标识输入框。

预期：申论反馈正常保存；七天计划包含专项、错题、考试和申论任务；API Key 仅用于本次请求。

### UI-011 自定义专项训练

1. 选择数量关系板块。
2. 设置 10 道题，难度范围 5～8。
3. 应用训练设置。

预期：生成 10 道数量关系题，页面显示每题和整套题的难度系数。

### UI-012 固定卷型模拟考试

1. 分别选择国考型和省考型。
2. 检查题量、时长、板块配额和板块顺序。
3. 生成模拟卷，作答一题后交卷。

预期：页面仅提供两种卷型，不提供自定义题量、时长和难度；国考型为 130 题/120 分钟，依次为政治理论20、常识15、言语30、数量10、判断35、资料20；省考型为 90 题/90 分钟，依次为政治10、常识5、言语15、数量15、判断25、资料20；政治与常识题池互斥，整卷无重复题和占位题，资料分析均为完整 5 题材料组；交卷成功并显示套题难度。

### UI-013 模拟考试恢复

1. 开始一场省考型模拟考试并选择一道题答案。
2. 刷新页面，再次进入模拟考试。
3. 检查倒计时、已作答数量和答案选择。

预期：未交卷考试自动恢复，已选答案仍然存在，可继续作答或主动放弃。

### UI-014 资料分析材料展示

1. 专项训练选择资料分析和 10 道题。
2. 生成题组并查看前后题目。

预期：展示公共材料文字、表格和图片；同一材料连续对应 5 道题。

### UI-015 注册、自动登录与重新登录

1. 在认证页点击切换到注册模式。
2. 使用“姓名或昵称”“邮箱”“密码”“确认密码”和可选目标考试填写表单。
3. 点击“注册并开始学习”，确认进入学习首页。
4. 退出当前账号。
5. 使用刚注册的邮箱和密码重新登录。

预期：注册成功后自动进入首页；公开注册用户身份为学员；退出后会话失效；重新登录成功且原账号资料可恢复；错误信息以可访问的警告区域展示。

### UI-016 注册页窄屏与新版视觉系统

1. 将浏览器视口分别调整为 390px 和 320px 宽。
2. 打开登录页并切换到注册模式。
3. 检查完整表单、密码显隐按钮、模式切换和主要提交按钮。
4. 登录后依次检查侧边导航、学习首页、专项训练和模拟考试配置区。

预期：页面不存在横向滚动；注册表单可纵向滚动且顶部字段不被截断；所有主要控件可见且触控区域足够；键盘焦点清晰；新版卡片、按钮、输入框和状态反馈在各核心页面保持一致。

### PLAN-007 学习计划程序验收

1. 创建包含结构化 `completionSpec` 的 schema v4 专项任务。
2. 尝试调用旧自我验收接口完成任务。
3. 分别完成同一资料的 4 道题和 5 道题并生成训练报告。
4. 使用报告 ID 调用系统验收接口，并并发重复提交通过证据。
5. 修改任务规则或文本快照后重新读取计划。

预期：旧自验收入口返回 `PROGRAM_ACCEPTANCE_REQUIRED`；4/5 不计完整材料组并返回结构化差距；5/5 可通过；并发请求只产生一条打卡和一条证据占用；规则或文本变化后旧打卡失效。

### PLAN-008 申论与正式模考程序验收

1. 以计划上下文启动广东省考 90 题正式模考并交卷。
2. 分别提交字数不足、超限、低分和达标的申论答案。
3. 尝试使用未绑定证据、其他用户证据或已占用证据验收。

预期：正式卷型、题量、会话和服务端时长均从原始考试会话核对；申论只核对字数、题目限字和分数，不伪造用时；未绑定、跨用户和重复证据均被拒绝。

### UI-024 程序验收移动端闭环

1. 在 320px 和 390px 打开 schema v4 程序任务卡片。
2. 检查“开始任务”“系统验收”触控尺寸，并确认不存在自我验收入口。
3. 启动真实专项、刷新页面、完成全部题目并返回计划。
4. 执行系统验收并检查证据摘要、目标值、实际值和差距显示。

预期：页面无横向溢出，计划配置被锁定且刷新后恢复；验收通过后显示 `PROGRAM_VERIFIED`，不提供撤销按钮，并同步移除 Android 提醒。

## 5. 工程质量检查

依次执行：

```bash
npx prisma validate
npm run lint
npm test
```

`npm test` 已包含生产构建、接口测试和浏览器测试。全部命令应以状态码 0 结束，且测试结束后端口 3107、3108 均被释放。

## 6. 验收判定规则

- P0：登录、题目字段保护、服务端判题、数据归属或生产构建失败，整轮不通过。
- P1：输入边界、统一错误结构、收藏、错题或统计失败，需要修复后复验。
- P2：提示文本、非核心视觉细节或文档偏差，可记录后进入下一小版本。

只有 P0、P1 用例全部通过，版本才可判定为正式验收通过。
