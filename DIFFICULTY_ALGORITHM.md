# 题目与套题难度算法

## 单题难度

每道题使用 `1.0～10.0` 的难度系数并保留一位小数。`1.0～3.9` 为基础，`4.0～6.9` 为进阶，`7.0～10.0` 为困难。

初始评分综合板块基础负荷、题型复杂度、题干阅读长度、数值信息密度、逻辑关系密度、选项数量和选项干扰强度。当一道题至少有 5 次真实作答后，系统加入经验校准：

```text
观测难度 = 1 + 9 × 贝叶斯收缩后的错误率
最终难度 = 65% × 内容难度 + 35% × 观测难度
```

错误率使用 12 次先验样本进行收缩，避免少量作答造成极端误判。扫描命令可重复执行：

```bash
npm run db:score:difficulty
```

## 套题难度

套题不使用题目难度的简单平均，因为大量简单题会掩盖少数高难题造成的时间压力和失分风险。

```text
套题难度 =
  55% × 2.4 阶广义均值
  + 25% × 第 80 百分位难度
  + 15% × 第 95 百分位难度
  + 5% × 难度离散压力
```

广义均值提高高难题权重；80 分位表示持续难题压力；95 分位反映顶端难题；离散压力用于识别难度起伏。结果限制在 `1.0～10.0` 并保留一位小数。

## 组题参数

`GET /api/questions/session` 支持 `count=5～100`、可选 `category`、`minDifficulty=1～10` 和 `maxDifficulty=1～10`，返回随机题组、符合条件的题库总量、每题难度和套题综合难度。
