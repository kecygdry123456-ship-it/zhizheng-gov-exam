# 知政公考系统迭代规划文档

> 文档版本：V1.0  
> 编写日期：2026-07-11  
> 适用范围：当前 Next.js + TypeScript + Tailwind CSS + Prisma + PostgreSQL 首版工程

> 状态更新：长期可用增强版已完成。当前包含 12224 道可用行测题、207 份完整资料分析材料、持久化训练配置、个性难度推荐、逐题模拟考试、答案自动保存和未交卷恢复。下文早期问题清单保留为历史规划记录，不再代表当前实现状态。

## 一、文档目的

本文档用于指导首版之后的产品和技术迭代，重点回答以下问题：

1. 当前版本已经具备什么能力，哪些能力仍属于演示实现。
2. 接下来应该优先修改哪些部分，以及为什么要这样排序。
3. 每个阶段需要调整哪些页面、服务端接口和数据表。
4. 每项工作完成后，应当使用什么标准进行验收。
5. 如何逐步把当前首版扩展为可持续运营的公考学习平台。

迭代时应遵循“先打通真实业务闭环，再丰富内容和智能能力，最后优化运营与规模化能力”的顺序，避免过早堆叠页面，却没有可靠的数据沉淀。

## 二、当前版本评估

### 2.1 已完成的基础能力

- 已建立 Next.js App Router 工程和 TypeScript 开发环境。
- 已安装并运行 PostgreSQL，使用 Prisma 管理数据模型。
- 已建立用户、分类、题目、试卷、作答记录和收藏等核心数据表。
- 已提供学员与管理员演示账号，并实现登录接口和会话 Cookie。
- 已完成学习首页、专项练习、模拟考试、错题收藏、学习分析和题库管理页面。
- 专项练习支持选择答案、即时评分、答案解析和智能助教式补充说明。
- 模拟考试支持倒计时、统一交卷和分数计算。
- 已实现题目、作答记录和收藏的服务端接口基础版本。
- 已通过 ESLint、TypeScript 和 Next.js 生产构建检查。

### 2.2 当前版本的主要限制

当前版本可以用于产品演示和交互确认，但还不适合作为正式运营版本，主要原因如下：

1. 前端题目仍以内置示例数据为主，没有完整读取数据库题库。
2. 学习记录、收藏和部分统计仍保存在浏览器本地，换设备后无法同步。
3. 前端主体集中在一个大型组件中，继续增加功能会显著提高维护成本。
4. 管理端只有列表和部分界面操作，新增、编辑、审核、批量导入尚未形成完整闭环。
5. 模拟考试还没有真实试卷编排、断点保存、到时自动交卷和详细报告。
6. 智能助教目前是规则化说明，尚未接入真实模型、上下文问答和用量控制。
7. 申论学习模块尚未实现。
8. 缺少注册、退出登录、个人资料、学习目标和账号管理功能。
9. 缺少统一的错误提示、加载状态、空数据状态和操作确认机制。
10. 缺少自动化测试、运行日志、数据备份和部署规范。

## 三、总体迭代方向与优先级

建议将后续工作划分为六个方向：

| 方向 | 目标 | 优先级 |
| --- | --- | --- |
| 真实数据闭环 | 所有练习、考试、收藏和统计统一写入数据库 | P0 |
| 工程结构治理 | 拆分组件、业务模块和公共服务，降低维护成本 | P0 |
| 题库与考试完善 | 建立可运营的题目录入、试卷编排和考试报告 | P1 |
| 申论与智能辅导 | 增加申论训练、批改建议和连续追问 | P1 |
| 用户与学习计划 | 完善账号体系、目标管理、提醒和个性化推荐 | P2 |
| 质量与运营能力 | 增加测试、日志、备份、内容审核和运营指标 | P2 |

P0 是正式使用前必须完成的内容；P1 决定产品是否具备公考特色；P2 用于提升长期留存和运营效率。

## 四、第一阶段：打通真实数据闭环（P0）

### 4.1 前端题库改为数据库驱动

当前 `exam-app.tsx` 中存在内置题目数组。下一步应删除业务层对内置题目的依赖，改为通过 `/api/questions` 获取数据。

具体修改：

- 为题目列表增加分类、题型、难度、状态、页码和每页数量参数。
- 服务端只向学员返回已发布题目。
- 答案和解析不应在学员开始作答前直接下发，应在提交答案后返回。
- 增加题目加载状态、加载失败提示、无题提示和重新加载按钮。
- 分类菜单从数据库读取，避免前端写死分类名称。
- 支持按模块、题型、难度和是否做过进行筛选。

建议接口：

```text
GET  /api/categories
GET  /api/questions?categoryId=&type=&difficulty=&page=&pageSize=
GET  /api/questions/:id
POST /api/questions/:id/answer
```

验收标准：刷新页面后题目仍来自数据库；新增并发布题目后，学员端无需改代码即可看到；未提交答案前无法从普通响应中直接获得正确选项。

### 4.2 作答记录统一持久化

当前练习记录主要保存在 `localStorage`。应改为登录后写入 `Attempt` 表，本地存储只用于短时间草稿和网络异常时的临时恢复。

需要记录：

- 用户、题目和作答模式。
- 用户选择、是否正确、作答耗时。
- 首次作答时间、最近作答时间和作答次数。
- 是否查看解析、是否请求智能说明。
- 练习会话编号，用于还原一次完整练习过程。

建议新增 `PracticeSession` 表，字段包括 `userId`、`categoryId`、`questionIds`、`currentIndex`、`startedAt`、`finishedAt` 和 `status`。

验收标准：同一账号在不同浏览器登录后可以看到一致的作答记录、错题和统计；刷新练习页后能够继续未完成的练习。

### 4.3 收藏与错题本改为服务端数据

收藏按钮应直接调用 `/api/favorites`。错题本不应单独复制题目，而应根据作答记录计算，并允许用户标记“已掌握”。

建议增加：

- 错题筛选：模块、题型、时间范围、错误次数、掌握状态。
- 错题复练：仅生成错题练习；连续两次答对后可建议标记为已掌握。
- 收藏备注：允许用户为收藏题目添加简短笔记。
- 批量操作：取消收藏、标记掌握、加入复习计划。

数据模型可增加 `WrongQuestionState`，保存 `userId`、`questionId`、`wrongCount`、`correctStreak`、`masteredAt` 和 `nextReviewAt`。

### 4.4 学习统计改为服务端聚合

当前能力数据和趋势图包含演示数值。应根据数据库作答记录计算：

- 今日、本周、本月答题量。
- 总正确率和各模块正确率。
- 平均作答时间及变化趋势。
- 连续学习天数。
- 模拟考试次数、平均分和最高分。
- 高频错误题型。
- 最近七天和最近三十天学习趋势。

建议接口：

```text
GET /api/statistics/overview
GET /api/statistics/trend?range=7d
GET /api/statistics/categories
GET /api/statistics/weak-points
```

所有统计接口应限定当前登录用户，并明确时间范围和时区。首页、学习分析页应共用同一套统计服务，避免数据口径不同。

## 五、第二阶段：工程结构治理（P0）

### 5.1 拆分大型前端组件

当前主要界面集中在 `src/components/exam-app.tsx`。建议按业务模块拆分：

```text
src/
  app/
    (auth)/login/
    (student)/dashboard/
    (student)/practice/
    (student)/exams/
    (student)/wrong-questions/
    (student)/statistics/
    admin/questions/
    admin/exams/
  components/
    layout/
    question/
    exam/
    statistics/
    ui/
  features/
    auth/
    practice/
    exam/
    favorite/
  lib/
    api/
    validation/
    services/
```

每个页面负责组合组件，业务请求放入独立服务，通用按钮、弹窗、表格和表单放入 `components/ui`。

### 5.2 建立统一类型和校验规则

- 从 Prisma 类型或共享类型文件生成前后端一致的数据结构。
- 所有写入接口使用 Zod 校验请求内容。
- 统一分页响应格式：`items`、`page`、`pageSize`、`total`。
- 统一接口错误格式：`code`、`message`、`details`。
- 对题干、选项、解析、用户昵称和备注设置长度限制。
- 对枚举值、页码、时间范围和排序字段进行明确校验。

### 5.3 会话与权限完善

目前已有登录和会话基础能力，后续需要补充：

- 获取当前用户接口与退出登录接口。
- 页面级登录检查，未登录时跳转到登录页。
- 管理页面的服务端角色检查。
- 会话过期后的统一提示和重新登录流程。
- 修改密码、找回密码和管理员重置流程。
- 管理员操作记录，包括操作人、对象、时间和变更摘要。

### 5.4 统一交互状态

所有异步操作应具备：加载中、成功、失败、重试、无数据五种状态。删除题目、交卷、离开未完成考试等重要操作需要二次确认。表单提交时应防止重复提交。

## 六、第三阶段：题库管理与模拟考试（P1）

### 6.1 完整题目管理

管理端需要从当前列表升级为可运营的内容系统：

- 新增和编辑单选题、多选题、判断题、材料题。
- 支持富文本题干、图片、表格和材料引用。
- 一个材料可关联多道小题。
- 支持草稿、待审核、已发布、已停用状态。
- 支持题目来源、年份、地区、考试类型和知识点标签。
- 支持复制题目和版本记录。
- 删除操作优先采用停用状态，保留历史作答关联。

建议扩展 `Question`：`source`、`year`、`region`、`examType`、`tags`、`materialId`、`reviewedBy`、`publishedAt`。新增 `QuestionMaterial` 和 `QuestionRevision` 表。

### 6.2 批量导入

支持管理员通过标准模板导入题目：

1. 下载模板。
2. 上传文件。
3. 服务端解析并校验。
4. 展示错误行和具体原因。
5. 管理员确认后写入数据库。
6. 生成导入结果报告。

首期可支持 CSV，后续增加 Excel。需要限制文件大小、行数和允许的字段，导入过程应使用事务保证一致性。

### 6.3 试卷编排

当前 `ExamPaper.questionIds` 可以用于首版，但长期应增加 `ExamPaperQuestion` 关联表，保存题目顺序、分值和所属部分。

管理端应支持：

- 手动选题和按规则随机组卷。
- 设置考试时长、总分、及格分和开放时间。
- 设置模块顺序、每题分值和题目排序。
- 预览试卷和复制试卷。
- 发布、下线和查看考试统计。

### 6.4 考试过程完善

- 每次开始考试创建 `ExamSession`。
- 每次选择答案后自动保存。
- 刷新页面或短暂离开后恢复剩余时间与答案。
- 使用服务端时间计算截止时间，避免仅依赖浏览器倒计时。
- 到达截止时间后自动提交。
- 支持题目导航、未答提醒、标记待检查和模块切换。
- 交卷后生成不可重复计算的固定成绩记录。

建议新增 `ExamSession`、`ExamAnswer` 和 `ExamResult` 表。

### 6.5 考试报告

报告应展示总分、排名区间、各模块得分、正确率、平均耗时、未答数量、同类用户参考值和重点复习建议。每道题应展示用户答案、正确答案、解析和知识点。

## 七、第四阶段：申论与智能辅导（P1）

### 7.1 申论题库

新增申论材料、题目和作答模块，至少支持：

- 归纳概括、综合分析、提出对策、贯彻执行和文章写作。
- 多段材料阅读、重点标记和个人笔记。
- 字数限制、答题计时、自动保存和历史版本。
- 参考答案、评分要点和示范文章。

建议新增 `EssayMaterial`、`EssayQuestion`、`EssaySubmission` 和 `EssayFeedback` 表。

### 7.2 智能批改

申论批改建议分层输出：

1. 基础检查：字数、段落、结构完整性。
2. 要点覆盖：与标准评分点进行对照。
3. 表达质量：逻辑、简洁性、规范性和衔接。
4. 修改建议：指出具体段落如何调整。
5. 示例改写：提供参考表达，但不直接替代用户完成整篇答案。

每次批改需要保存模型版本、提示模板版本、输入摘要、输出结果、耗时和用量，以便后续评估质量和成本。

### 7.3 行测连续问答

将当前规则化说明升级为连续对话：

- 用户可以追问“为什么其他选项不对”“还有更快的方法吗”“这类题如何识别”。
- 对话上下文包含题干、选项、正确答案、官方解析和用户选择。
- 回答采用提示、步骤、结论三层结构。
- 控制单次回答长度和每日使用次数。
- 对模型不可用或响应超时提供固定解析作为备用结果。

建议新增 `TutorConversation` 和 `TutorMessage` 表，并将模型调用封装在独立服务中，方便更换提供方。

### 7.4 个性化学习建议

推荐逻辑初期不必完全依赖模型。可以先根据错误次数、正确率、作答耗时和最近复习时间生成可解释的规则推荐，再由模型把结果组织为自然语言。

推荐结果应明确说明原因，例如：“最近七天数量关系正确率为 42%，低于个人平均值 18 个百分点，因此建议优先练习基础计算题。”

## 八、第五阶段：用户体系与学习计划（P2）

### 8.1 账号与个人中心

- 用户注册、退出登录和资料编辑。
- 目标考试、考试日期、每日题量和可学习时间设置。
- 头像、昵称和地区等非必要资料应允许用户选择是否填写。
- 学习数据导出与账号注销流程。

### 8.2 学习计划

根据考试日期和当前能力生成周计划：

- 每日专项练习量。
- 每周模拟考试安排。
- 错题复习任务。
- 申论写作任务。
- 任务完成状态和延期处理。

新增 `StudyPlan`、`StudyTask` 和 `StudyCheckIn` 表。首页的“今日学习计划”应由真实任务生成，不再使用固定项目。

### 8.3 提醒与消息中心

首期实现站内消息，后续再评估邮件或其他通知方式。消息包括计划提醒、考试结果、内容更新和系统通知。用户应能设置接收时间和消息类型。

## 九、质量、性能与运行维护（P2）

### 9.1 自动化测试

建议建立三层测试：

- 单元测试：评分函数、统计计算、时间计算和校验规则。
- 接口测试：登录、题目查询、提交答案、收藏和管理员操作。
- 浏览器流程测试：登录、完成练习、参加考试、查看报告和管理端录题。

关键业务发布前必须覆盖：答案判定、考试截止时间、成绩计算、权限判断和数据隔离。

### 9.2 性能优化

- 题库列表和作答记录必须分页。
- 为高频查询字段增加数据库索引。
- 首页统计结果可短时间缓存，作答后主动更新。
- 图表组件按需加载。
- 大型材料、图片和富文本资源使用对象存储。
- 批量导入、大规模统计和报告生成放入后台任务。

建议重点索引：`Question(categoryId,status)`、`Attempt(userId,createdAt)`、`Attempt(userId,questionId)`、`ExamSession(userId,status)`。

### 9.3 运行日志和监控

应记录接口错误、数据库慢查询、模型调用失败、批量导入结果和管理员重要操作。日志中不记录用户密码、完整会话值或不必要的个人信息。

需要设置：

- 服务可用性检查接口。
- 数据库连接检查。
- 接口响应时间和错误比例统计。
- 模型调用耗时与费用统计。
- 磁盘、数据库容量和备份状态提醒。

### 9.4 数据备份与恢复

- 每日自动备份 PostgreSQL。
- 保留最近七天每日备份和最近三个月每周备份。
- 定期验证备份文件能否恢复。
- 正式环境与测试环境使用不同数据库和配置。
- 数据表结构变更使用 Prisma Migration，不再直接使用 `db push`。

## 十、建议迭代排期

以下排期按一名全职开发人员估算，可根据团队规模并行调整。

### Sprint 1：真实练习闭环（1～2 周）

- 拆分登录、布局、首页和练习页面。
- 题目改为数据库读取。
- 提交答案写入数据库。
- 收藏与错题改为服务端数据。
- 增加当前用户和退出登录接口。

交付结果：用户可以登录、从数据库练习、刷新后保留记录，并在不同设备查看一致数据。

### Sprint 2：真实统计与管理端（1～2 周）

- 建立统计聚合接口。
- 首页与学习分析页移除演示数值。
- 完成题目新增、编辑、状态切换和分页检索。
- 增加管理员操作记录。

交付结果：学习数据真实可信，管理员可以独立维护题库。

### Sprint 3：模拟考试闭环（2 周）

- 完成试卷编排。
- 增加考试会话、答案自动保存和时间控制。
- 完成自动交卷、固定成绩和详细报告。

交付结果：可正式组织一次完整模拟考试。

### Sprint 4：申论首版与智能辅导（2～3 周）

- 建立申论题库和在线作答。
- 接入智能批改服务。
- 增加行测连续追问和用量统计。

交付结果：系统同时覆盖行测与申论核心学习场景。

### Sprint 5：学习计划与质量建设（1～2 周）

- 增加个人目标、每日任务和站内提醒。
- 补充自动化测试、日志、备份和部署流程。
- 优化移动端体验和加载性能。

交付结果：形成可持续迭代和小规模正式运营的版本。

## 十一、下一轮应立即执行的任务清单

建议下一轮开发严格按以下顺序开始：

1. 将 `exam-app.tsx` 拆成路由页面和可复用组件。
2. 增加 `/api/auth/me` 与 `/api/auth/logout`，建立页面登录检查。
3. 修改题目接口，提交前不返回答案和解析。
4. 专项练习改为读取数据库并提交真实作答记录。
5. 收藏按钮接入数据库接口。
6. 错题本根据数据库记录生成，并增加掌握状态。
7. 建立统计服务，移除首页和分析页中的固定演示数据。
8. 完成管理端题目新增、编辑、发布和停用表单。
9. 为以上流程编写接口测试和浏览器流程测试。
10. 使用正式迁移文件记录数据表变更。

上述十项完成后，系统才算从“交互演示首版”进入“真实可用首版”。模拟考试、申论和智能批改应建立在这一数据闭环之上。

## 十二、版本完成定义

每次迭代只有同时满足以下条件，才可以标记为完成：

- 功能符合产品验收场景，不只有静态界面。
- 数据真实写入数据库，刷新和重新登录后仍然存在。
- 服务端对输入进行校验，并返回统一错误信息。
- 学员和管理员的数据与权限边界正确。
- 桌面端和移动端均可完成核心流程。
- ESLint、TypeScript 和生产构建通过。
- 关键流程具备自动化测试。
- 数据表调整包含迁移文件和必要的回退说明。
- README、接口说明和本迭代文档同步更新。

这套完成标准应作为后续所有需求的共同验收基线。
